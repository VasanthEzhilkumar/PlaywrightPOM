{
  "compilerOptions": {
    "baseUrl": ".", 
    "esModuleInterop": true,
    //"allowSyntheticDefaultImports": true,                                     /* Specify the base directory to resolve non-relative module names. */
    "paths": {                                           /* Specify a set of entries that re-map imports to additional lookup locations. */
      // "@pages/*":["pageFactory/pageRepository/*"],
      // "@objects/*":["pageFactory/objectRepository/*"],
      "@lib/*":["src/library/*"],
      "@Utils/*":["src/base/Utils/*"],
      "@uiConstants/*":["src/base/constants/*"],
      "@Excel/*":["@types/convert-excel-to-json"]
    }                                         
  }
}
